# 第 4 步：保存配色 + 随机选色按钮

> ⚠️ **已废弃（2026-05-30）**
> 按钮已重新设计：
> - **Save 按钮** → 取消，功能由 Strip 左侧圆形+号图标（`createNewProject()`）替代
> - **随机配色按钮** → 从 Header 移到 action-bar（3D 下方、Strip 上方）
> - 详见 `计划8-UI整体化.md`

---

## 原计划内容（仅供存档）

原计划内容记录在 2026-05-30 之前的版本。

### 功能确认

| 按钮 | 功能 | 状态 |
|---|---|---|
| 保存配色 | 截取 3D Canvas 缩略图，保存方案到 localStorage | ❌ 已废弃 |
| 随机配色 | 随机打乱所有部件颜色 | ✅ 保留，位置变更 |

### 相关文件变更

- `app_mobile_new.js` — `saveCurrentProject()` 废弃，`createNewProject()` 替代
- `app_mobile_new.js` — `randomizeColors()` 保留，事件绑定移到 action-bar
- `index_mobile.html` — `#btn-save` 删除，新增 `.btn-add-project` 圆形+号

### 文档链接

- 新计划：`计划8-UI整体化.md`（第 7 步）
- 配色库逻辑：`计划3-配色库.md`（第 5 步）