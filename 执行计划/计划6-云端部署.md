# 第 9 步：云端部署（最终上线）

> 第 8 步（数据整合）完成后执行

---

## 步骤目标

**完成标志**：
1. 所有步骤（第 1-7 步）代码已就绪并验证
2. 部署到 Cloudflare Pages 成功
3. 线上地址可访问，内容与本地预览一致

---

## 部署信息

| 属性 | 值 |
|---|---|
| 平台 | Cloudflare Pages |
| 项目名 | safan-color-picker |
| 部署命令 | `npx wrangler@3 pages deploy . --project-name=safan-color-picker` |
| 线上地址 | https://safan-color-picker.pages.dev |
| 部署目录 | 项目根目录（`/Users/mini/Documents/Projects/Safan_ColorPicker/`） |
| 入口文件 | `index_mobile.html` |

---

## 部署流程

1. 确认所有步骤（第 1-7 步）已完成并通过本地预览验证
2. 执行部署命令
3. 验证线上地址可访问
4. 通知用户线上地址

---

## 依赖

- 第 8 步（数据整合）已完成并验证通过
- `colors.js` 已创建并被两端引用
- `npx wrangler@3` 已安装（或可用）
- Cloudflare Pages 项目 `safan-color-picker` 已创建（部署过一次后 account_id 已缓存）

---

## 验证方式

第 8 步完成后：

1. 访问 https://safan-color-picker.pages.dev/index_mobile.html
2. 所有功能与本地预览一致
3. 手机可直接扫码或点击链接访问