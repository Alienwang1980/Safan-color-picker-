# 第 6 步：配色库详情

> ⚠️ **已废弃（2026-05-30）**
> 详情页设计已整合到 `计划3-配色库.md`（第 5 步）作为全屏详情页。
> 详情不再是在 `#detail-area` 内嵌展开，而是独立全屏覆盖层（`#detail-container`）。
> 详见 `计划3-配色库.md` 修改 6（openDetailPage）。

---

## 原计划内容（仅供存档）

原设计：点击展开 `#detail-area` 显示详情列表。

### 新设计差异

| 项目 | 原设计 | 新设计 |
|---|---|---|
| 触发方式 | 点击"查看详情"按钮 | 点击焦点卡片 |
| 显示方式 | `#detail-area` 内嵌展开（max-height） | `#detail-container` 全屏覆盖 |
| 详情内容 | 色块 + 部件名 + 颜色名 + 编号 | 同左 |
| 删除入口 | 详情区内按钮 | 详情页底部按钮 |

### 文档链接

- 新计划：`计划3-配色库.md`（第 5 步）— openDetailPage() 全屏详情