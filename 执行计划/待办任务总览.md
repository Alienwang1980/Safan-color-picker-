# 待办任务总览

> 更新于 2026-05-31，优先级从高到低

---

## 高优先级（用户明确要求）

### 1a. 随机配色增加"从收藏中随机"选项
随机按钮增加两个选项/模式：
- **随机所有配色**（当前行为，从所有颜色中随机）
- **随机我的配色**（仅从 `safan_favorites` 收藏列表中随机选取颜色）
- UI 方式：点击随机按钮时弹出选择，或增加切换开关

### 1c. 随机配色不再创建新方案
**现状**：`randomizeColors()` 末尾调用了 `this.createNewProject()`，每次随机都创建一个新方案。
**修正**：删除第1314-1315行的 `this.createNewProject()` 调用，随机只修改当前方案颜色。
在配色库/选色面板中，已被收藏的颜色上显示收藏标记。
- 方案：右上角桃心 ❤️ / 红点
- 判断依据：`localStorage` key `safan_favorites` 中的 hex 列表
- 涉及文件：`app_mobile_new.js`（渲染色块时检测并添加标记）

### 2. Bug 修复清单（计划 A）
| # | 标题 | 严重度 | 涉及文件 |
|---|------|--------|----------|
| 1 | 玉石白与白色 hex 完全重复 | 🔴 | `colors.js` |
| 5 | 选色后自动关闭 Modal，无法连续选色 | 🟡 | `app_mobile_new.js` |
| 10 | 删除项目后模型颜色残留 | 🟡 | `app_mobile_new.js` |
| 15 | 详情页删除按钮点击文字不触发 | 🟡 | `app_mobile_new.js` |
| 8 | 配色库预览色块 CSS 高度未定义（不可见） | 🔵 | `index_mobile.html` |

### 3. 暗色模式（延后，不着急）

---

## 中优先级

### 4. 计划 A 其余 Bug（按修复意愿排序）
| # | 标题 | 严重度 |
|---|------|--------|
| 6 | 初始加载方案预览色全灰误导 | 🟡 |
| 19 | 全屏点击关闭 Modal 时也收了 DetailArea | 🔵 |
| 22 | 选色对话框关闭后 emissive 高亮残留 | 🔵 |
| 2 | `collapseBottomStrip()` 重复定义 | 🟡 |
| 3 | `applyProjectColors()` 并发动画/颜色格式不统一 | 🟡 |
| 4 | `randomizeColors()` Fan 颜色不合法 | 🟡 |
| 7 | Fan 颜色约束仅在加载时存在 | 🔵 |
| 12 | Three.js 版本过旧（r128） | 🔵 |
| 13 | `preserveDrawingBuffer: true` 不必要 | 🔵 |
| 17 | 保存/加载的 hex 大小写不统一 | 🔵 |
| 18 | `FAN_COLORS` 格式不统一 | 🔵 |
| 14 | `deleteCurrentProject()` 与方法死代码 | 🔵 |
| 16 | `submitProject()` 空占位符 | 🔵 |
| 20 | localStorage key typo "sidan" | 🔵 |
| 21 | HTML 缺少 loading 元素 | 🔵 |
| 23 | opacity=0 无 CSS transition | 🔵 |
| 24 | `<a>` 链接缺 rel 属性 | 🔵 |
| 25 | iOS safe-area 未适配 | 🔵 |
| 26 | 死 HTML/死代码 | 🔵 |
| 27 | `colorGroups` 无防御性检查 | 🔵 |
| 28 | `__BATTERY_COLORS` 未引用 | 🔵 |
| 29 | 双重 `pointerMoved` 检查 | 🔵 |
| 30 | 每次遍历 mesh 取颜色 | 🔵 |
| 31 | `state.isEditMode` 未预初始化 | 🔵 |
| 32 | 代码风格不一致 | 🔵 |

### 5. 数据整合（原第9步）
从 `colors.js` 等源文件抽取颜色数据，统一格式。

### 6. Cloudflare Pages 部署（原第10步）
将项目部署上线。

---

## 低优先级/已取消

- ~~**长按编辑态** — 用户表示已不需要，取消~~
- ~~**Dark 主题** — 用户表示不着急，延后~~
