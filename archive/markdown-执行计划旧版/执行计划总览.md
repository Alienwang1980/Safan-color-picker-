# Safan Color Picker 手机版 — 完整执行计划总览

> 目标：将 PC 版完整功能移植到手机版（index_mobile.html + app_mobile_new.js）
> 文档状态：总览文档（已更新 2026-05-30）
> ⚠️ 重大变更：计划 A/C 跳过、计划 B 取消（移动端交互不同）、新增计划 F
> 执行顺序：D（第1阶段）→ F（第2阶段）→ E（第3阶段）

---

## 一、计划清单

| 计划 | 文档 | 核心任务 | 状态 |
|---|---|---|---|
| **A** | 执行计划A-配色核心.md | 修复 applyColor + 实现 Reference 模式 | ⛔ 已跳过 |
| **B** | 执行计划B-保存加载系统.md | saveCurrentProject + 删除UI + Detail区 | ⛔ 已取消 |
| **C** | 执行计划C-配色模式切换UI.md | 新增 Official/Reference 切换按钮 | ⛔ 已跳过 |
| **D** | 执行计划D-配色库缩略图.md | 配色库卡片增加3D缩略图（增强现有Project系统） | ✅ 第 1 阶段 |
| **F（新增）** | 执行计划F-移动端长按编辑态.md | 长按卡片 → 编辑态 → 删除/编辑按钮 | 📋 待创建 |
| **E** | （Dark 主题在第 3 阶段） | Dark 主题 + 下载链接 | 第 3 阶段 |

---

## 二、执行顺序图

```
┌──────────────────────────────────────────┐
│  Layer 0 基础交互层 ✅ 已完成              │
│  Three.js 场景 / 模型加载 / 点击高亮      │
└──────────────────┬───────────────────────┘
                   │
         ┌─────────┴─────────┐
         ▼                   ▼
┌─────────────────┐    ┌─────────────────┐
│   第 1 阶段     │    │   计划 F        │
│   计划 D        │    │   移动端长按编辑 │
│   Profile 快照  │    │   📋 待创建      │
│   🔴 最高优先级 │    │                 │
│                 │    │ 长按卡片         │
│ saveProfile     │    │ → 编辑态         │
│ restoreProfile  │    │ → 删除/编辑按钮  │
│ deleteProfile   │    │                 │
│ Profile Modal   │    │                 │
└────────┬────────┘    └────────┬────────┘
         │                      │
         └──────────┬───────────┘
                    ▼
┌─────────────────────────────────┐
│   第 3 阶段                      │
│   Dark 主题 + 部署               │
│   ⚪ 低优先级                    │
│                                 │
│ CSS 变量主题切换                 │
│ Cloudflare Pages 部署           │
└─────────────────────────────────┘
```

**说明**：计划 D（Profile 快照）是最优先要实现的功能。

---

## 三、各计划核心产出

### 计划 A：配色核心
⛔ **已跳过** — 手机版无 Reference mode，仅原色模式

---

### 计划 B：保存/加载系统
⛔ **已取消** — 移动端交互与 PC 不同，原计划的删除按钮/返回按钮/Detail区 chip点击均不适用

---

### 计划 C：配色模式切换 UI
⛔ **已跳过** — 依赖计划 A，手机版仅原色模式不需要切换

---

### 计划 D：Profile 快照系统（第 1 阶段）
**文件改动：app_mobile_new.js + index_mobile.html**

| 方法 | 内容 |
|---|---|
| `_loadProfiles()` / `_saveProfiles()` | localStorage `sidan_profiles_mobile` |
| `saveProfile()` | 保存当前状态缩略图 |
| `restoreProfile()` | 动画恢复快照（使用简单 applyColor 签名） |
| `deleteProfile()` | 删除快照 |
| `showProfileModal()` | 浮动按钮 + Modal 展示 |

**验证**：右下角浮动按钮点击 → Modal 显示缩略图卡片 → 点击恢复

---

## 四、代码改动量预估

| 计划 | app_mobile_new.js 改动 | index_mobile.html 改动 | 难度 |
|---|---|---|---|
| A | ⛔ 已跳过 | — | — |
| B | ⛔ 已取消 | — | — |
| C | ⛔ 已跳过 | — | — |
| D | ~80 行 | ~60 行 HTML + CSS | 🟡 中 |
| F | 📋 待创建 | — | — |
| **合计** | **~80 行** | **~60 行** | |

---

## 五、完整代码文件清单

手机版涉及的文件：

| 文件 | 作用 |
|---|---|
| `app_mobile_new.js` | 手机版主逻辑（754 行） |
| `index_mobile.html` | 手机版 HTML 页面（496 行） |
| `app_v2.js` | PC 版参考（1,367 行）— 复制粘贴素材 |

配色数据（内联在 `index_mobile.html` 的 `<script>` 中）：
- `window.__COLOR_GROUPS` — 配色库（Basic 33色 / Matte 25色 / Translucent 10色）
- `window.__BATTERY_COLORS` — 电池颜色（5色）